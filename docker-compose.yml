version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: warps
    ports:
      - '5432:5432'
    volumes:
      - warps_pg_data:/var/lib/postgresql/data

volumes:
  warps_pg_data:
